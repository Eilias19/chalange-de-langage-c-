#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i
    int n ,somme = 0;
    printf("entrez le nombre d'elements: ");
    scanf("%d",&n);
    int T[i];
    for(int i = 0 ; i < n ; i++){
        printf("entrez l'element %d: ", i + 1 );
        scanf("%d", &T[n]);
    }
    for(int i =0; i < n ; i++);
        {
        somme += T[i];
    }
    printf("la somme est :%d\n",somme);
    return 0;
}
